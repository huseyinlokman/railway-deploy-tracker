# Railway Deploy Tracker

A small Flask app that listens for Railway deployment webhooks and sends Slack notifications.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt